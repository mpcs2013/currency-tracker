namespace CurrencyTracker.Api;

/// <summary>
/// Empty type whose only purpose is to anchor architecture-test
/// assembly lookups via <c>typeof(ApiAssemblyAnchor).Assembly</c>.
/// Do not give this type members; do not reference it from production
/// code other than the architecture tests themselves.
/// </summary>
public sealed class ApiAssemblyAnchor;
