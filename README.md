## Getting started

    git clone https://github.com/mpcs2013/currency-tracker.git
    cd currency-tracker
    dotnet tool restore

`dotnet tool restore` installs the pinned local tools (CSharpier 1.2.6).
Phase 0.B will expand this section once the rest of the repo scaffolding lands.